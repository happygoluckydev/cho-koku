# Anthropic API（Claudeとの対話・ブラッシュアップに使用）
ANTHROPIC_API_KEY=

# Supabase（アイデアの記録先。サーバー側のAPIルートからのみ使用する）
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# /api/chat を保護する簡易トークン。クライアントとサーバーで同じ値を共有する
APP_API_TOKEN=
