# Anthropic API（Claudeとの対話・ブラッシュアップに使用）
ANTHROPIC_API_KEY=

# Supabase（アイデアの記録先。サーバー側のAPIルートからのみ使用する）
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# 認証は Cloudflare Access に委ねるため、アプリ側の共有トークンは持たない（docs/cloudflare-setup.md）。
# 本番（Workers）ではこれらを `wrangler secret put <NAME>` で登録する。
