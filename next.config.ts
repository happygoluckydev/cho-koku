import type { NextConfig } from "next";

// Cloudflare Workers（@opennextjs/cloudflare）上でNext.jsのApp Router + API Routesを動かすための設定。
// OpenNext 導入時に必要な設定（initOpenNextCloudflareForDev 等）は公式ドキュメントで確認して追加する。
const nextConfig: NextConfig = {};

export default nextConfig;
